%%-*- mode:erlang -*-

% superl

{lib_dirs, ["deps"]}.

{erl_opts, [ {i, "deps"} ]}.

{deps, [
    {pose, ".*",
        {git, "git://github.com/beadsland/pose", {branch, master}}}
    ]}.
